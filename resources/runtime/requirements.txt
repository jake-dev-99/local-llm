# Baseline runtime. Optimisation-specific packages (bitsandbytes, torchao, AWQ,
# GPTQ, flash-attn, optimum*) are deliberately absent: they are added only when
# a real model requires them.
torch==2.14.0
transformers==5.17.0
accelerate==1.15.0
safetensors==0.8.0
# Marker-gated transitive deps, pinned explicitly: pip evaluates dependency
# markers against the HOST interpreter rather than the target, so resolving on
# anything newer than 3.10 silently drops `python_version < "3.11"` deps
# (exceptiongroup via anyio, tomli via huggingface-hub) and Windows-only
# colorama. Explicit pins are always downloaded, whatever the host.
exceptiongroup==1.3.1
tomli==2.4.1
colorama==0.4.6
